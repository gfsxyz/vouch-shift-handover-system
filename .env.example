# Copy to .env.local (gitignored). Required to ingest the free-text night log:
# every night log goes through a live Claude Sonnet 4.6 extraction call.
# Without it, the JSON pipeline still produces a handover and the log is flagged "not ingested".
ANTHROPIC_API_KEY=sk-ant-...
